/*function GetButton(){
return Bot.getProperty("button", {
  balance: "💰 {balanceid} {cur}",
  deposit: "📥 Deposits",
  reinvest: "♻️ Reinvest",
  payment: "📤 Withdraws",
  status: "📉 Status",
  referral: "🏄 Referrals",
  wallet: "💼 Wallets",
  calco: "🖥 Calculateds",
  support: "📞 Supports",
  abouts: "‼️ Abouts",
  livepay: "⚜ Live Pays",
  faq: "📰 Faqs",
  investmenthist: "📉 My Investments",
  withrowalshist: "📈 My Withdrawals",
  accounthist: "🗂 Historys",
  accounthistb: "🗂 Historys",
  walletb: "💼 Wallets",
  bonus: "🎁 My Bonus",
  back: "⬅️ Returns"
})
}




function ReturnButton(){
var type = message;
var buttonOn = GetButton()
  
  
  
  if(type==buttonOn.balance.slice(0,1)){
   return 
   }
if(type==buttonOn.deposit.slice(0,1)){
   return 
   }  
  if(type==buttonOn.reinvest.slice(0,1)){
   return 
   }
  if(type==buttonOn.payment.slice(0,1)){
   return 
   }
  if(type==buttonOn.status.slice(0,1)){
   return 
   }
  if(type==buttonOn.referral.slice(0,1)){
   return 
   }
  if(type==buttonOn.wallet.slice(0,1)||type==buttonOn.walletb){
   return 
   }
  if(type==buttonOn.calco.slice(0,1)){
   return 
   }
  if(type==buttonOn.support.slice(0,1)){
   return 
   }
  if(type==buttonOn.abouts.slice(0,1)){
   return 
   }
  if(type==buttonOn.livepay.slice(0,1)){
   return 
   }
  if(type==buttonOn.faq.slice(0,1)){
   return 
   }
  if(type==buttonOn.investmenthist.slice(0,1)){
   return 
   }
  if(type==buttonOn.withrowalshist.slice(0,1)){
   return 
   }
  if(type==buttonOn.accounthist.slice(0,1)||type==buttonOn.accounthistb.slice(0,1)){
   return 
   }
  if(type==buttonOn.bonus){
   return 
   }
if(type==buttonOn.back){
   return 
   }
  
}


publish({
 
  
})
*/
